to building container image
% container build --platform=linux/amd64 . -t opend
 
to running container
place config.xml file and rsa pem into ./data folder
for xml config:
    - ip needs to be 0.0.0.0
% container run --rm -it --name opend -p 11111:11111 -c 2 -m 1024M -v `pwd`/data:/root/.com.futunn.FutuOpenD opend

