syntax = "proto2";
package futupb;

import "Types.proto";

option go_package = "github.com/santsai/futu-go/pb";

message QotUpdateBrokerResponse {
  required Security security = 1; //股票
  optional string name = 4; // 股票名称
  repeated Broker brokerAskList = 2; //经纪Ask(卖)盘
  repeated Broker brokerBidList = 3; //经纪Bid(买)盘
}

message QotUpdateBrokerResponse_Internal {
  required RetType retType = 1 [default = RetType_Unknown]; //RetType,返回结果
  optional string retMsg = 2;
  optional int32 errCode = 3;

  optional QotUpdateBrokerResponse payload = 4;
}
